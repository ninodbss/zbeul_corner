# Marble Race – CML Sound Picker (MVP)

MVP website:
- Users log in with TikTok (Login Kit for Web).
- Users pick a “Commercial Music Library” track from your curated list (no scraping).
- Your live/game bridge can query the chosen track by `open_id`.

## Run
```bash
cp .env.example .env.local
npm install
npm run dev
```
